# Python / Bytecode
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
*.pyd

# Virtual environments
venv/
.env
.venv/
churn/                   # ← your custom venv name

# Jupyter / Notebooks
.ipynb_checkpoints/
*.ipynb_checkpoints/

# Data – ignore large/raw files, but keep DVC tracking files
data/raw/*
!data/raw/*.dvc          # keep .dvc pointer files
data/processed/*
!data/processed/*.dvc    # keep DVC tracking for processed if needed

# MLflow experiments & artifacts (very large – never commit)
mlruns/
mlflow/

# DVC cache (never commit – huge and local)
.dvc/cache/
*.dvc.cache

# Model artifacts – usually generated (but allow if small for deployment)
models/*.pkl             # ← XGBoost model
models/*.joblib
data/processed/*.pkl     # ← fitted preprocessor
data/processed/*.joblib

# If you want to ALLOW committing small artifacts (recommended for portfolio/demo):
# Remove or comment out the lines above and add:
# !models/xgboost_model.pkl
# !data/processed/preprocessor.pkl

# Logs & temp files
*.log
*.tmp
*.bak
.DS_Store                # macOS
Thumbs.db                # Windows

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo

# Docker / Build artifacts
docker-compose.override.yml
*.dockerignore.backup

# Testing / Coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Dependency management
Pipfile.lock
poetry.lock

# macOS / Windows junk
.DS_Store
ehthumbs.db