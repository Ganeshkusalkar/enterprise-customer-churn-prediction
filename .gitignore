# Python bytecode / compiled files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
*.pyd

# Virtual environments
venv/
.env
.venv/
churn/                   # your custom venv folder

# Jupyter / Notebooks temporary files
.ipynb_checkpoints/
*.ipynb_checkpoints/

# Data – ignore large/raw files, keep DVC tracking
data/raw/*
!data/raw/*.dvc

# Processed data – ignore by default, but allow specific deployment files
data/processed/*
!data/processed/preprocessor.pkl   # ← explicitly allow for Render deployment

# Models – ignore by default, but allow specific deployment files
models/*
!models/xgboost_model.pkl          # ← explicitly allow for Render deployment

# MLflow runs & artifacts (very large, never commit)
mlruns/
mlflow/

# DVC cache (local, huge – never commit)
.dvc/cache/
*.dvc.cache

# Logs, temp, backups
*.log
*.tmp
*.bak

# OS-specific junk
.DS_Store                # macOS
Thumbs.db                # Windows
ehthumbs.db

# IDE / editor files
.vscode/
.idea/
*.swp
*.swo

# Testing / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Docker / build temp
docker-compose.override.yml
*.dockerignore.backup

# Dependency locks (if not using)
Pipfile.lock
poetry.lock